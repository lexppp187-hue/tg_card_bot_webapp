import React, {useEffect, useState} from 'react'
export default function Inventory(){const [inv,setInv]=useState({coins:0,cards:[]});useEffect(()=>{fetch('/api/inventory',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({initDataUnsafe: window.Telegram?.WebApp?.initDataUnsafe || {}})}).then(r=>r.json()).then(setInv)},[]);return (<div><h3>Inventory — coins: {inv.coins}</h3><div className='card-grid'>{inv.cards.map(c=>(<div className='card' key={c.id}><img src={c.image||'https://via.placeholder.com/300x200?text=Card'} alt={c.name}/><div><b>{c.name}</b></div><div style={{color:'#9ca3af'}}>[{c.rarity}] x{c.qty}</div></div>))}</div></div>)}
